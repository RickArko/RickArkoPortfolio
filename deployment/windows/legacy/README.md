# Windows Legacy Scripts

These PowerShell scripts are archived for reference only.

The active workflow for this repository is Linux-first:

- use Bash scripts from `deployment/bin/`
- use the top-level `Makefile`
- on Windows, run everything through WSL rather than native PowerShell

If you still need one of the old scripts temporarily, treat it as deprecated and expect it to drift.

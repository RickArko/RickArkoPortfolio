version: 1.0
runtime: docker

run:
  runtime-version: latest
  command: uv run gunicorn -w 4 -b 0.0.0.0:8080 --chdir src app:app
  network:
    port: 8080
  env:
    - name: FLASK_ENV
      value: production